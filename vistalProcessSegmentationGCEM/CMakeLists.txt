## #################################################################
## Generated by dtkPluginGenerator
## #################################################################

cmake_minimum_required(VERSION 2.8.1)

project(vistalProcessSegmentationGCEMPlugin)

## #################################################################
## Setup version numbering
## #################################################################

set(${PROJECT_NAME}_VERSION_MAJOR 2 CACHE STRING "${PROJECT_NAME} major version number.")
set(${PROJECT_NAME}_VERSION_MINOR 0 CACHE STRING "${PROJECT_NAME} minor version number.")
set(${PROJECT_NAME}_VERSION_BUILD 0 CACHE STRING "${PROJECT_NAME} build version number.")
set(${PROJECT_NAME}_VERSION
  "${${PROJECT_NAME}_VERSION_MAJOR}.${${PROJECT_NAME}_VERSION_MINOR}.${${PROJECT_NAME}_VERSION_BUILD}")

mark_as_advanced(${PROJECT_NAME}_VERSION_MAJOR)
mark_as_advanced(${PROJECT_NAME}_VERSION_MINOR)
mark_as_advanced(${PROJECT_NAME}_VERSION_BUILD)

set(CMAKE_COLOR_MAKEFILE ON)
set(CMAKE_VERBOSE_MAKEFILE OFF)
set(CMAKE_INCLUDE_CURRENT_DIR TRUE)

## #################################################################
## Resolve dependencies
## #################################################################

find_package(dtk REQUIRED)

include(${dtk_USE_FILE})
include(dtkPlugin)

# vistal
find_package(Vistal REQUIRED)
include(${VISTAL_USE_FILE})

# medinria
find_package(medinria REQUIRED)
include(${medinria_USE_FILE})



#set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${CMAKE_SOURCE_DIR}/cmake")

# mstools
#    find_package(MSTools REQUIRED)

FIND_PATH(MSTOOLS_SOURCE_DIR "/usr/include" DOC "Path to MS tools source dir")
FIND_PATH(MSTOOLS_LIB_DIR "/usr/lib" DOC "Path to MS tools build")


SET(MSTOOLS_INC ${MSTOOLS_SOURCE_DIR}/Libraries/Classification/
    ${MSTOOLS_SOURCE_DIR}/Libraries/Common/
    ${MSTOOLS_SOURCE_DIR}/Libraries/MeanShift/
    ${MSTOOLS_SOURCE_DIR}/Libraries/nico/)

SET(MSTOOLS_LIB ${MSTOOLS_LIB_DIR})

include_directories(${MSTOOLS_INC})

LINK_DIRECTORIES(${MSTOOLS_LIB_DIR})

## #################################################################
## Input
## #################################################################

set(${PROJECT_NAME}_HEADERS_MOC
  vistalProcessSegmentationGCEMPlugin.h
  vistalProcessSegmentationGCEMToolBox.h
  vistalProcessSegmentationGCEM.h )

set(${PROJECT_NAME}_HEADERS sgc.h
  ${${PROJECT_NAME}_HEADERS_MOC})

set(${PROJECT_NAME}_SOURCES
  vistalProcessSegmentationGCEMPlugin.cpp
  vistalProcessSegmentationGCEMToolBox.cpp
  vistalProcessSegmentationGCEM.cpp)

## #################################################################
## Build rules
## #################################################################

qt4_wrap_cpp(${PROJECT_NAME}_SOURCES_MOC ${${PROJECT_NAME}_HEADERS_MOC})

add_library(${PROJECT_NAME} SHARED
  ${${PROJECT_NAME}_SOURCES}
  ${${PROJECT_NAME}_SOURCES_MOC}
  ${${PROJECT_NAME}_HEADERS})

target_link_libraries(${PROJECT_NAME}
  ${QT_LIBRARIES}
  mstools
  ${VISTAL_LIBS}
  dtkCore
  medCore
  medGui)

dtk_build_doc(vistalProcessSegmentationGCEMPlugin)


include_directories(${MSTOOLS_INC})

add_executable(${PROJECT_NAME}App
  vistalProcessSegmentationGCEMTestingApp.cpp
)

target_link_libraries(${PROJECT_NAME}App
 ${QT_LIBRARIES}
  mstools
  ${VISTAL_LIBS}
  dtkCore
  medCore)

## #################################################################
## Install rules
## #################################################################

install(TARGETS ${PROJECT_NAME}
  RUNTIME DESTINATION plugins
  LIBRARY DESTINATION plugins
  ARCHIVE DESTINATION plugins)
